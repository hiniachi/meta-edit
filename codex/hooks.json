{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|apply_patch|Edit|Write|MultiEdit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "p=\"$(find \"${CODEX_HOME:-$HOME/.codex}/plugins/cache\" -path \"*/meta-edit/0.9.3/dist/codex/deny-raw-edit.js\" -type f -print -quit)\"; if [ -n \"$p\" ]; then exec node \"$p\"; else printf '%s' '{\"decision\":\"block\",\"reason\":\"meta-edit Codex deny-raw-edit hook binary was not found in the plugin cache; failing closed. Reinstall the meta-edit Codex plugin.\"}'; fi",
            "statusMessage": "Checking meta-edit declaration"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "p=\"$(find \"${CODEX_HOME:-$HOME/.codex}/plugins/cache\" -path \"*/meta-edit/0.9.3/dist/codex/session-onboarding.js\" -type f -print -quit)\"; if [ -n \"$p\" ]; then exec node \"$p\"; else exit 0; fi",
            "statusMessage": "Loading meta-edit onboarding"
          }
        ]
      }
    ]
  }
}
